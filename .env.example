# Backend API base URL.
# Override per developer: copy this file to `.env` and edit.
# On Android emulator use http://10.0.2.2:8083, on iOS simulator http://localhost:8083,
# on a real device use your machine's LAN IP (e.g. http://192.168.1.10:8083).
EXPO_PUBLIC_API_URL=http://localhost:8083
